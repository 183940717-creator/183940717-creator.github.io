这个文件夹专门放首页首屏素材。

固定文件名：

1. home-background.jpg
首页首屏背景图。
后续想替换背景时，把新图片改名为 home-background.jpg，覆盖这个文件即可。

2. lx-studio-wordmark.svg
首页主标题 “LX Studio” 的 SVG 字标。
后续想替换标题样式时，把新的 SVG 改名为 lx-studio-wordmark.svg，覆盖这个文件即可。

3. site-logo.svg
左上角导航 LOGO。
后续想替换 LOGO 时，把新的 SVG 改名为 site-logo.svg，覆盖这个文件即可。

注意：
- 文件名尽量保持完全一致。
- 如果背景不是 jpg，可以告诉我，我会帮你把网站改成 png / webp。
